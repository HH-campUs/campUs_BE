export interface topic {
    topic: string;
    // lookUp: number;
}

export interface trip {
    tripId? : number;
    userId? : number;
    campId? : number;
    address? : string;
    date?: number;
}